# ONNIT — 남은 일

> 2026-09-05 기준. 끝난 일은 체크하고, 새로 생기면 아래에 붙인다.

## 지금 막혀 있는 것

**대표 결정이 없으면 다음으로 못 넘어가는 것들이다.** 나머지 항목보다 이게 먼저다.

- [ ] **홈페이지 방향 확정** — 세 가지 중 둘은 **일단 문서 두 장에 맞춰 적용해 두었다.**
  보고 아니라고 하시면 되돌린다.
  - ~~이름을 화면 폭에 꽉 채울지~~ → **되돌렸다 (2026-09-04).** 간판을 걷어내고
    로고를 네비 한 줄 왼쪽에 넣었다. "너무 블로그 같다"는 판정이었다
  - ~~강조 줄을 명조체로 둘지~~ → 명조체로 했다. README 디자인 방향 3번대로
  - 파랑을 본문 글자에까지 쓸지, 버튼과 선에만 쓸지 — **아직 안 정했다**
- [ ] **사업자등록번호 · 주소 · 전화** — 2026-09-05 에 **임시값을 아예 걷어냈다.**
  `000-00-00000` 같은 값을 공개 페이지에 두는 것보다 비워두는 편이 낫다고 판단했다.
  실제 값이 나오면 `.info` 목록에 다시 넣는다.
  지금 회사 정보에 남은 것: 회사명 · 대표 · 사업 영역 · 설립(2026년 9월 7일) · 이메일.
  시안 마커가 0개가 되어 꼬리말 안내와 `.ph` · `.draft` CSS 도 함께 제거했다.
- [ ] **지메일에서 `lnk@` 로 답장하기** — 수신은 2026-09-05 에 끝났다(아래 참고).
  다만 Email Routing 은 **받기 전용**이라, 지금 지메일에서 답장하면 보내는 사람이
  `lian.dy220@gmail.com` 으로 나간다. 문의를 받아놓고 개인 지메일로 답하는 모양이 된다.
  지메일 → 설정 → 계정 → **"다른 주소에서 메일 보내기"** 에 `lnk@onnit.co.kr` 을 추가한다.
  확인 메일은 Email Routing 이 지메일로 넘겨주므로 인증이 어렵지 않다.
  이건 Cloudflare 가 아니라 지메일 쪽 설정이고 무료다.
- [ ] **`www` HTTPS 인증서** — CNAME 은 2026-09-05 에 넣었고 `http://www` 는 apex 로
  301 을 정상적으로 보낸다. 다만 **GitHub 인증서에 `www` 가 안 들어갔다.**
  GitHub 은 인증서를 발급하던 시점에 있던 도메인만 넣는데, `www` 를 그 뒤에 추가했다.
  커스텀 도메인을 지웠다 다시 넣어 재검사를 걸었지만(19:4x) 아직 반영되지 않았다.
  `gh api repos/lian220/onnit/pages` 의 `https_certificate.domains` 로 확인한다.
  안 풀리면 `www` CNAME 을 빼는 쪽이 낫다 — 인증서 경고보다 "주소 없음"이 낫다.
- [ ] **로고의 전원 기호를 계속 쓸지** — 마케터가 바꾸라고 판정했다. 근거 셋.
  - **선례 40곳 이상.** Gateway · Indesit · Kogan · Staples EasyTech, 워드마크형인 Com On 까지
  - **상표권이 안 생긴다.** IEC 60417-5009 표준기호라 상표법 33조 1항 6호
    "간단하고 흔히 있는 표장"에 걸린다. `⏻NNIT` 결합은 등록되지만 식별력을 글자가 공급하는
    것이라 **기호 부분엔 독점권이 없다.** 경쟁사가 같은 기호를 써도 못 막는다
  - **포지셔닝과 반대 방향.** 이 회사는 "안 켜는 게 나으면 그렇게 말합니다"를 파는데
    전원 기호는 "켜 드립니다"를 말한다. 애써 좁힌 포지셔닝을 도로 넓힌다
  - 제안받은 대안: **세로획만 빼기.** 형태는 90% 그대로 남고 뜻이
    "켰다" → "아직 안 닫았다 = 고를 여지"로 바뀐다. 위 셋을 동시에 턴다
  - 검증법: 중소기업 의사결정자 8~10명에게 마크만 2초 보여주고 "무슨 회사 같나" 자유응답.
    "전자제품/전원/IT일반"이 절반 넘으면 판정이 데이터로 확정된다
- [ ] **파비콘을 마크로 둘지 `ON` 글자로 바꿀지** — 16px 에서는 도형이 무엇이든 뭉갠다.
  지금은 마크를 쓰는데, 탭에서 보이는 건 "ONNIT"이 아니라 파란 사각형 안의 범용 아이콘이라
  회수되는 브랜드 자산이 없다. `ON` 두 글자면 읽히고 이름 앞 두 글자라 자산이 쌓인다.
- [ ] **최종 스택** — 정적 HTML 을 유지할지 Next.js 로 옮길지. **당분간 정적으로 간다.**
  `onboarding.html` 이 "Node.js 는 필요 없다"를 전제로 쓰여 있어서, 빌드가 생기면
  합류자의 진입 비용이 그만큼 늘어난다. 페이지가 3장이라 컴포넌트로 회수할 것도 적다.
  옮길 신호는 셋 중 하나가 오면 — 인사이트 글 5편 초과 · 페이지 10장 초과 ·
  구축 사례가 목록/상세 구조로 갈 때. 그때 후보는 Next.js 보다 **Astro** 가 먼저다.
- [x] ~~`onnit` 저장소 공개 여부~~ — 공개로 돌리고 GitHub Pages 로 배포했다.

---

## ONNIT 홈페이지

- [ ] **`index.html` 본문 조판** — 상단과 뼈대는 끝났다. 헤더·네비·명조 강조·
  섹션 배경 리듬·브레이크포인트 2종까지.
  남은 것은 **1440px 에서 히어로 텍스트가 왼쪽 45% 에만 몰리는 문제**다.
  `.hero-in{position:relative;padding:96px 0 84px}` 뿐이고 오른쪽 칼럼이 선언된 적이
  없어서 생긴 일이라, 거기에 무엇을 놓을지 정해야 풀린다.

- [ ] **GitHub Pages 를 계속 쓸지 Workers 로 옮길지** — [`DEPLOY.md`](DEPLOY.md) 참고.
  Cloudflare Pages 는 후보에서 뺐다. Cloudflare 가 신규 프로젝트는 Workers 로 시작하라고 안내한다.
  갈림길은 하나다 — **비개발자가 머지 전에 화면을 확인할 수 있어야 하느냐.**
  필요하면 옮기고(브랜치별 프리뷰 배포), 아니면 지금이 충분하다.
  덤으로 풀리는 것: 문의 폼 백엔드, `onnit.co.kr/wedding` 경로.
- [ ] **인사이트 첫 글** — 지금은 제목 세 개만 있고 전부 `준비 중`이다.
  인증, 결제 멱등, 관측성 중 하나를 실제로 쓴다.
- [ ] **문의 폼 백엔드** — 지금은 내용을 복사해 메일로 보내는 방식이다.

## 청첩장

- [ ] **참석 여부와 방명록 실제 저장** — 지금은 각자 브라우저에만 남는다.
  저장 경계를 `read()` / `write()` 두 함수로 좁혀 두었으니 Supabase 테이블 두 개로 교체하면 된다.
- [ ] **짧은 주소** — 기본 데이터만으로 주소가 2,100자를 넘는다.
  서버에 저장하고 짧은 키를 내주는 구조가 정답이다. 위 항목과 같이 간다.
- [ ] **갤러리 교체 기능** — 사진 밴드 세 장이 고정이다.
  사진은 주소에 담을 수 없어 저장소가 생겨야 풀린다.
- [ ] **카카오톡 공유** — 지금은 링크 복사와 Web Share API로만 처리한다. SDK를 붙이면 미리보기 카드가 뜬다.
- [ ] **타임라인 이후 항목 편집** — 인사말·타임라인·계좌는 제작 페이지에서 되지만
  엔딩 문구와 교통 안내는 아직 마크업에 박혀 있다.

## 문서

- [ ] **`basics.html`·`onboarding.html` 메타** — doctype, `lang="ko"`, description,
  OG 태그가 없다. 링크를 공유하면 미리보기가 안 뜬다. 위와 같은 이유로 미뤘다.
  파비콘은 2026-09-04 헤더 정리 때 넣었다.

- [ ] **`CLAUDE.md` 작성** — 코딩 규칙을 저장소에 둔다.
  그래야 합류하는 사람의 Claude가 같은 규칙으로 코드를 쓴다.
  온보딩 문서의 '대표님이 해야 할 것'에 적어 둔 항목이다.
- [ ] **기초 문서 2편** — 지금 `basics.html`은 서버와 배포까지다.
  git과 브랜치, PR이 무엇인지가 다음 편으로 필요하다. 개발까지 시킬 거면 반드시 있어야 한다.
- [ ] **온보딩 문서 윈도우 판** — 지금은 맥 전용이다. 나중에 다른 사람을 받을 때.

## 팀과 운영

- [ ] **협업자 초대** — GitHub 사용자명을 받아야 한다.
  `gh api -X PUT repos/lian220/onnit/collaborators/<사용자명>`
- [ ] **Slack 워크스페이스** 생성과 초대. GitHub 앱을 붙이면 PR 알림이 채널로 들어온다.
- [ ] **Notion 워크스페이스** 초대.
- [ ] **Claude 요금 주체 결정** — 무료 플랜으로는 Claude Code를 못 쓴다. Pro 이상이 필요하다.
  회사에서 낼 거면 팀 플랜으로 묶는 편이 정산이 깔끔하다.
- [ ] **`main` 브랜치 보호** — 직접 푸시를 막고 PR을 거치게 한다.

---

## 끝난 것

- [x] **구축 사례에 청첩장 추가** (2026-09-05) — 기능 나열이 아니라 판단을 적었다.
      상용 다섯 곳 전부 첫 화면이 랜딩이었던 이유("보는 사람과 만드는 사람이 다르다"),
      기능 개수 대신 하객 관점 셋을 고른 것, 랜딩에 지어낸 실적 대신 못 하는 것 셋을
      적은 것까지. 열어보기·제작 페이지 링크를 달았다 — 사례 중 유일하게 만져볼 수 있다
- [x] **`wedding.onnit.co.kr` 연결** (2026-09-05) — CNAME 한 줄. 상대경로라 `/make.html` 과
      `css/` 자원이 그대로 동작한다. HTTPS 강제까지 켰다

- [x] **청첩장 `wedding.onnit.co.kr` 연결** — CNAME 한 줄로 붙였다. 상대경로라
      `/make.html` 과 `css/` 자원이 그대로 동작한다. HTTPS 강제까지 켰다
- [x] **`onnit.co.kr` 도메인 연결** — 가비아에서 네임서버를 Cloudflare 로 위임하고
      A 4개 · AAAA 4개를 넣었다. 프록시는 껐다 — 켜면 GitHub Pages 가 인증서를 못 받는다.
      `og:image` · `canonical` · README 주소도 새 도메인으로 갱신했다
- [x] **로고** — O 를 전원 기호로 대체. 지름 96 · 획 17 · NNIT SemiBold 600 · 틈 82°.
      `logo.svg` · `mark.svg` · `favicon.svg` · `og.png`.
      글자는 fonttools 로 패스화해 폰트 의존을 없앴다. 파비콘은 별도 판으로 그렸다
- [x] **`lnk@onnit.co.kr` 메일 수신** (2026-09-05) — Cloudflare Email Routing 으로 붙였다.
      메일함을 산 게 아니라 대표 지메일로 **전달**하는 것이고, 인증된 목적지로 가는
      전달은 모든 플랜에서 무료다.
      - 목적지 `lian.dy220@gmail.com` 등록 — 계정 소유 주소라 즉시 인증됐다
      - `wrangler email routing enable onnit.co.kr` → MX 3개(`route1~3.mx.cloudflare.net`)와
        SPF(`v=spf1 include:_spf.mx.cloudflare.net ~all`) 가 자동으로 들어갔다
      - 규칙 `lnk` : `to:lnk@onnit.co.kr` → `forward:lian.dy220@gmail.com`
      - **Catch-all 은 꺼져 있고 동작은 drop** 이다. 즉 `lnk@` 외의 주소
        (`contact@`, `info@` 등)로 온 메일은 전달되지 않고 버려진다. 필요하면 그때 켠다
      - A/AAAA 는 그대로라 사이트는 영향 없다 (확인함, 200)
- [x] **`onboarding.html` 모바일 수정** (2026-09-04) — viewport 메타가 없어 휴대폰에서
      980px 를 축소해 보여주던 것과, 390px 에서 930px 로 넘치던 것을 함께 잡았다.
      원인은 `.item{grid-template-columns:52px 1fr}` 의 칼럼 기본값 `min-width:auto` 라
      긴 명령어가 축소를 막고 있었다. `.phase-h>*,.item>*{min-width:0}` 으로 낮추니
      930 → 393px. 남은 3px 은 `.mono` 안의 공백 없는 긴 경로였고
      `overflow-wrap:anywhere` 로 마무리했다.
      **390px·320px 모두 넘침 0건**, 코드 블록 10개는 각자 가로 스크롤로 갇혔다.
      `basics.html` 은 표가 이미 스크롤 컨테이너 안이라 손댈 게 없었다
- [x] **색 토큰을 한 벌로** (2026-09-04) — 세 파일에 3벌로 흩어져 있던 색 정의를
      `docs/css/tokens.css` 한 장으로 모았다. 빌드는 없고 `<link>` 한 줄로 문다.
      이름 체계도 통일했다 — 문서 두 장이 쓰던 `--soft`·`--muted`·`--ok`·`--on` 을
      `--surface`·`--ink-muted`·`--signal`·`--on-ink` 로 바꿨다 (49곳).
      **이제 파랑을 바꾸려면 한 곳만 고치면 된다**
- [x] **문서 두 장 헤더를 홈과 통일** (2026-09-04) — `basics`·`onboarding` 의 로고가
      실은 `<text>` 로 그린 가짜였다. 전원 기호가 없고 폰트가 안 뜨면 깨진다.
      홈과 같은 패스 로고로 바꾸고 크기(`height:26px`)와 한 줄 구조를 맞췄다.
      홈으로 돌아가는 링크가 없던 것도 이때 붙였다. 파비콘 링크도 추가
- [x] **헤더를 한 줄로 통합** (2026-09-04) — 간판 워드마크(1128×293) + 태그라인 줄 +
      별도 네비바 3단을 한 줄로 합쳤다. 로고 `height:26px` 왼쪽, 링크, 오른쪽 끝 CTA.
      태그라인 두 줄은 지웠지만 내용이 `hero-eyebrow` 와 `hero-sub` 에 이미 있어 손실은 없다.
      `<div class="navbar">` 는 `<header>` 로 승격
- [x] **모바일 네비를 햄버거로** (2026-09-04) — 링크 7개 가로 스크롤을 접었다.
      링크가 "서비스·관점" 둘만 보이던 문제가 사라졌다. 열면 링크 7개 + 풀폭 CTA.
      `aria-expanded`·`aria-controls`, Esc 로 닫고 포커스 복귀, 링크 고르면 자동으로 닫힘.
      데스크톱은 래퍼를 `display:contents` 로 둬서 기존 한 줄 레이아웃이 그대로 산다.
      **맞바꾼 것: JS 가 꺼지면 모바일 메뉴가 안 열린다** (예전 가로스크롤은 JS 없이 됐다)
- [x] **배경 흰색 고정** (2026-09-04) — `index`·`basics`·`onboarding` 세 장에서
      `prefers-color-scheme:dark` 자동 전환을 걷어내고 `color-scheme:light` 를 박았다.
      다크모드 맥에서도 흰 배경이다. `[data-theme="dark"]` 팔레트는 남겨 뒀다
- [x] 홈페이지 얼굴 맞춤 — 문서 두 장과 같은 간판 헤더, 명조 강조, 색값 25곳 정렬
- [x] 홈페이지 모바일 파손 수정 — viewport 누락, 앵커 가림, iOS 폼 강제 확대, 모바일 내비 부재
- [x] 홈페이지 브레이크포인트 4종 → 2종 (640 · 960)
- [x] 홈페이지 doctype · `lang="ko"` · description · OG · canonical · 파비콘

  위 넷 모두 `docs/index.html` 한 파일에만 적용했다.

- [x] 레퍼런스 조사 1차 — 개발 컨설팅·SI 회사 다섯 곳 (`refboard.html`)
- [x] 레퍼런스 조사 2차 — Landbook·Awwwards·Recent에서 넷 (`gallery-picks.html`)
- [x] 홈페이지 1차 시안 (`index.html`) — 방향 확정 전 초안
- [x] 합류 준비물 체크리스트, 맥 기준 (`onboarding.html`)
- [x] 서버·배포 기초 문서 (`basics.html`) — 그림 네 장
- [x] 청첩장 데모와 제작 페이지 — 별도 저장소, 로그인 없이 공개 배포 완료
- [x] 브랜드명 ONIT → ONNIT 전면 교체, 도메인 onnit.co.kr 반영
- [x] 홈페이지·기초문서·온보딩 GitHub Pages 배포 — 로그인 없이 열린다.
      공개할 것만 `docs/` 에 두고 조사 자료는 저장소에만 남겼다
